import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const SiteFooter = () => {
  const style = {
    textAlign: "center"
  };
  return <Footer style={style}>Mini Banking ©2019 Created by ABC</Footer>;
};

export default SiteFooter;
